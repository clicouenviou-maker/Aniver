rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir acesso total sem validação de autenticação (conforme solicitado pelo usuário)
      allow read, write: if true;
    }
  }
}
